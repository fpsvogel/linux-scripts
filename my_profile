# My startup scripts.
# To use, add this line to ~/.profile (assuming the present directory is "~/dotfiles"):
#   source ~/dotfiles/my_profile

for f in ~/dotfiles/profile*/*; do source $f; done

for f in ~/dotfiles/sudo_profile*/*; do sudo $f; done
