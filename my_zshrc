# My Zsh customizations.
# To use, replace the plugins section and Zsh initialization in ~/.zshrc
# with this line (assuming this is in "~/dotfiles"):
#   source ~/dotfiles/my_zshrc

for f (~/dotfiles/zshrc*/*) source $f

# Clone https://github.com/fpsvogel/aliases into ~/aliases
source ~/aliases/aliases
